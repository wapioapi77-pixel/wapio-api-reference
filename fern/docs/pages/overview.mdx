---
title: Overview
---

Official public API reference for Wapio.

This reference includes the customer-facing Wapio API surface for sessions, messages, contacts, groups, and channels / communities.

Set the `apiToken` environment variable to either a Personal Access Token (`bps_pat_*`) or a session-scoped key (`bps_sk_*`).
